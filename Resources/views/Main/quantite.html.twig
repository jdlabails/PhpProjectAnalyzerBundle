<div class="panel panel-default">
    <div class="panel-heading">
        <h3>
            <span class="glyphicon glyphicon-list"></span>
            {% trans %}quantity.quantity{%endtrans %}
            <span class="dropdown optionsTool" style="float: right; display: none">
                <button class="btn btn-default dropdown-toggle optionsTool" style="border:none" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
                    <span class="glyphicon glyphicon-option-vertical"></span>
                </button>
                <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                    <li role="presentation">
                        <a role="menuitem" tabindex="-1" href="#">
                            <span class="glyphicon glyphicon-fullscreen" ></span>Voir les rapports
                        </a>
                    </li>
                    <li role="presentation">
                        <a role="menuitem" tabindex="-1" href="#">
                            <span class="glyphicon glyphicon-repeat" ></span> Relancer la compta
                        </a>
                    </li>
                </ul>
            </span>
        </h3>
        <div style=" font-size: 75%;color: #777;">
            {% trans %}quantity.srcContent{%endtrans %}
        </div>
    </div>
    <div class="panel-body">
        <ul class="list-group">
            
            {% if projectAnalyzer.isEnable('loc') %}
            
            <li class="list-group-item">
                <span class="badge alert-info">{{ projectAnalyzer.analyze.loc}}</span>
                {% trans %}quantity.codeLines{%endtrans %}
            </li>
            
            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbClasses}}</span>
                {% trans %}quantity.classes{%endtrans %}
            </li>

            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbMethod}}</span>
                {% trans %}quantity.methods{%endtrans %}
            </li>

            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbNamespace}}</span>
                {% trans %}quantity.namespaces{%endtrans %}
            </li>

            {% endif %}

            {% if projectAnalyzer.isEnable('count') %}

            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbBundles}}</span>
                {% trans %}quantity.bundles{%endtrans %}
            </li>
            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbDir}}</span>
                {% trans %}quantity.directories{%endtrans %}
            </li>
            <li class="list-group-item">
                <span class="badge alert-info">{{projectAnalyzer.analyze.nbfile}}</span>
                {% trans %}quantity.files{%endtrans %}    
                <span class="badge outil">
                    <span class="glyphicon glyphicon-search" onclick="$('#listeNbFile').toggle()"></span>
                </span>
                <ul id="listeNbFile" class="metricDetail">
                    <li class="list-group-item">
                        <span class="badge">{{projectAnalyzer.analyze.nbPhpFile}}</span>
                        {% trans %}quantity.PHP{%endtrans %}
                    </li>
                    <li class="list-group-item">
                        <span class="badge">{{projectAnalyzer.analyze.nbCSSFile}}</span>
                        {% trans %}quantity.CSS{%endtrans %}
                    </li>
                    <li class="list-group-item">
                        <span class="badge alert-warning">{{projectAnalyzer.analyze.nbCSSLib}}</span>
                        {% trans %}quantity.libCSS{%endtrans %}
                    </li>
                    <li class="list-group-item">
                        <span class="badge">{{projectAnalyzer.analyze.nbJSFile}}</span>
                        {% trans %}quantity.JS{%endtrans %}
                    </li>
                    <li class="list-group-item">
                        <span class="badge alert-warning">{{projectAnalyzer.analyze.nbJSLib}}</span>
                        {% trans %}quantity.libJS{%endtrans %}
                    </li>
                    <li class="list-group-item">
                        <span class="badge">{{projectAnalyzer.analyze.nbTwig}}</span>
                        {% trans %}quantity.twig{%endtrans %}
                    </li>
                </ul>
            </li>

            {% endif %}
            
        </ul>
    </div>
</div>
