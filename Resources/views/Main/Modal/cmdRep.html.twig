<div class="modal fade" aria-hidden="true" id="cmdRep_{{idAnalyse}}">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h3 class="panel-title">{{title}}</h3>
                <small>{{report['date']}}</small>
            </div>
            <div class="modal-body">
                <div class="panel-heading">
                    <h3 class="panel-title">Voici la commande à lancer</h3>
                </div>
                <div class="panel-body">
                  <code>{{report['cmdRep']|nl2br}}</code>
                </div>              
            </div>
            <div class="modal-footer">
                <button  type="button" class="btn btn-warning" onclick="$('#cmdRep_{{idAnalyse}}').modal('hide');lancerUneAnalyse('{{idAnalyse}}')">Relancer</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
