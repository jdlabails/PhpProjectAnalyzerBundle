language: php

php:
    - 5.6
    - 7.0
    
matrix:
  include:
    - php: 5.6
      env:
        - COVERALLS=1
        - PHPCS=1
    
before_install:
    - wget http://getcomposer.org/composer.phar    

before_script:
    - php composer.phar install --dev
#    - pyrus install pear/PHP_CodeSniffer
#    - phpenv rehash

script:
    - ls -la
    - ls -la bin
    - vendor/bin/phpunit -c phpunit.xml
    - vendor/bin/phpcs --standard=PSR2 --extensions=php Entities Manager Command Controller DependencyInjection Traits

notifications:
  email:
    - jd.labails@gmail.com
